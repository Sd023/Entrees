<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Entreés" parent="Theme.Material3.Dark.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="colorPrimary">@color/primary_light</item>
        <item name="bottomNavigationStyle">@style/MyBottomNavigationView</item>
        <item name="android:statusBarColor">@color/FloralWhite</item>
    </style>

    <style name="Theme.Entreés" parent="Base.Theme.Entreés" />

    <style name="MyBottomNavigationView" parent="Widget.Design.BottomNavigationView">
        <item name="android:background">@color/black</item>
        <item name="android:elevation">8dp</item>
        <item name="itemTextColor">@color/green</item>
        <item name="itemIconTint">@color/YellowGreen</item>
    </style>

</resources>